@model IEnumerable<string>

@Html.ActionLink("Home","List", "Clothes", null,
	new { @class = "menuHeader" })

@foreach (var link in Model)
{
	@Html.RouteLink(link, new
	{
		controller = "Clothes",
		action = "List",
		category = link,
		page = 1
	}, new
	{
		@class = "menuHeader"
	})
}