@model Domain.Entities.Wear

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-danger">@Model.Price.ToString("# USD")</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>
